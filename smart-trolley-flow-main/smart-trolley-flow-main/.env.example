# Auth service base URL (no trailing slash). Default: http://localhost:8081
VITE_AUTH_API_URL=http://localhost:8081

# App API base URL (products, trolleys, alerts, dashboard, sales). Same as auth-service if all APIs run there. If unset, mock data is used.
# VITE_APP_API_URL=http://localhost:8081
